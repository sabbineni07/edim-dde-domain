sources:
  edim_sql_wh:
    type: databricks_sql
    server_hostname: ${DATABRICKS_HOST}
    http_path: ${DATABRICKS_HTTP_PATH}
    # Auth is not configured here. Tokens are resolved at runtime:
    #   1) Databricks Apps — user OAuth via API middleware (ContextVar)
    #   2) Local dev — `az login` (DefaultAzureCredential)
