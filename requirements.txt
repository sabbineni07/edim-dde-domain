# Domain package. Install framework + optional Databricks SQL + Foundry LLM:
-e ../edim-dde-ai[azure-search,cosmos,postgres]
pydantic>=2.0
pydantic-settings>=2.0
PyYAML>=6
databricks-sql-connector>=3.0.1
azure-identity>=1.15
openai>=1.40
# Explicit pins for StateStore / checkpointer host-run (also via edim-dde-ai[postgres])
psycopg[binary]>=3.1
psycopg-pool>=3.2
langgraph-checkpoint-postgres>=2.0
