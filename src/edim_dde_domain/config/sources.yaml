sources:
  edim_sql_wh:
    type: databricks_sql
    server_hostname: ${DATABRICKS_HOST}
    http_path: ${DATABRICKS_HTTP_PATH}
    # Auth at runtime: Apps user OAuth (middleware) or local `az login`
